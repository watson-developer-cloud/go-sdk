# This is a weird way of telling Travis to use the fast container-based test
# runner instead of the slow VM-based runner.
sudo: false

language: go
node_js: "8.3"

# Only the last two Go releases are supported by the Go team with security
# updates. Any older versions be considered deprecated. Don't bother testing
# with them.
go:
  - 1.10.x

before_install:
  - nvm install 8.3
  - npm install -g npm@6.x
  - go get -u github.com/onsi/ginkgo/ginkgo
  - go get -u github.com/onsi/gomega/...
  - go get -u github.com/modocache/gover
  - go get -u github.com/cloudfoundry-community/go-cfenv
  - go get -u github.com/go-resty/resty
  - go get -u github.com/stretchr/testify/assert
  - go get -u github.com/go-openapi/strfmt
  - go get -u github.com/parnurzeal/gorequest
  - go get -u gopkg.in/alecthomas/gometalinter.v2
  - go get -u golang.org/x/lint/golint

# Skip the install step. Don't `go get` dependencies. Only build with the code
# in vendor/
install:
  - git clone --depth=1 --branch=$TRAVIS_BRANCH git@github.ibm.com:arf/go-sdk.git $GOPATH/src/go-sdk

# Don't email me the results of the test runs.
notifications:
  email: false

# script always runs to completion (set +e). If we have linter issues AND a
# failing test, we want to see both. Configure golangci-lint with a
# .golangci.yml file at the top level of your repo.
script:
  - gometalinter.v2 $GOPATH/src/go-sdk/...  # run a bunch of code checkers/linters in parallel NOTE: Only golint enabled for now
  - $GOPATH/bin/ginkgo -r  # Run all the tests (based on testing detailed in SDK README.md)

# TODO Enable semantic release below once we are public.
# deploy:
# - provider: script
#   skip_cleanup: true
#   script: npx travis-deploy-once "npx semantic-release --debug"
